# The directory Mix will write ebin files to.
/_build/

# The directory Mix downloads your dependencies sources to.
/deps/

# Where third-party dependencies like ExDoc output generated docs.
/doc/

# Ignore .fetch files in case you ever run `mix deps.fetch`
/.fetch

# If the VM crashes, it generates a dump, let's ignore it too.
erl_crash.dump

# Ignore package tarball (built via `mix hex.build`).
shopping_cart-*.tar

# Ignore assets that are produced by build tools.
/priv/static/assets.json
/priv/static/cache_manifest.json

# Ignore digested assets cache.
/priv/static/digest_cache.json

# In case you use Node.js/npm, you want to ignore its stuff. But be sure to
# commit your deps using `mix deps.get --only prod` before deploying!
# Uncomment this and change the path if you use npm.
# /assets/node_modules

# Temporary files, for example, from tests.
/tmp/

.env
.env.local
